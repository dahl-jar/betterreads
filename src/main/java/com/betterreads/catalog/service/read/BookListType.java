package com.betterreads.catalog.service.read;

/** A homepage book list. */
public enum BookListType {

    /** Books most recently added to the catalog. */
    RECENTLY_ADDED,

    /** Best-rated books with enough ratings to be meaningful. */
    TOP_RATED
}
