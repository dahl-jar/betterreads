<?xml version="1.0"?>
<ruleset name="BetterReads Strict PMD Rules"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0
                             https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

    <description>Strict PMD ruleset for BetterReads: catches magic strings, duplication, complexity, and bad practices.</description>

    <!-- ===================== Best Practices ===================== -->
    <rule ref="category/java/bestpractices.xml">
        <!-- Too noisy for Spring Boot with Lombok -->
        <exclude name="GuardLogStatement"/>
        <!-- Re-included below with MockMvc-aware config -->
        <exclude name="UnitTestShouldIncludeAssert"/>
        <!-- Re-included below with a higher cap; integration tests for side-effecting flows
             validate user state + side-effect tables + response, which exceeds the default 4. -->
        <exclude name="UnitTestContainsTooManyAsserts"/>
    </rule>

    <!-- MockMvc's andExpect chain is a real assertion; PMD doesn't recognize it by default. -->
    <rule ref="category/java/bestpractices.xml/UnitTestShouldIncludeAssert">
        <properties>
            <property name="extraAssertMethodNames" value="andExpect,andExpectAll,satisfies"/>
        </properties>
    </rule>

    <!-- Integration tests for register/verify/reset legitimately assert: HTTP status, user row
         state, side-effect table state (refresh tokens, outbox, etc.). Default of 4 fires on
         legitimate chained-fact tests. Bumping to 8 still catches tests that genuinely test
         too many things at once. -->
    <rule ref="category/java/bestpractices.xml/UnitTestContainsTooManyAsserts">
        <properties>
            <property name="maximumAsserts" value="8"/>
        </properties>
    </rule>

    <!-- ===================== Code Style ===================== -->
    <rule ref="category/java/codestyle.xml">
        <exclude name="AtLeastOneConstructor"/>
        <exclude name="OnlyOneReturn"/>
        <exclude name="CommentDefaultAccessModifier"/>
        <!-- Long variable names are fine for readability -->
        <exclude name="LongVariable"/>
        <!-- Re-included below to allow nested @Nested grouper classes (Me, Login, Register) -->
        <exclude name="ShortClassName"/>
        <!-- Re-included below: MockMvc tests statically import get/status/content/jsonPath/matchers -->
        <exclude name="TooManyStaticImports"/>
        <!-- Re-included below to allow the documented two-letter identifiers id and url -->
        <exclude name="ShortVariable"/>
    </rule>

    <!-- Allow the documented short identifiers like id and url; flag single letters. -->
    <rule ref="category/java/codestyle.xml/ShortVariable">
        <properties>
            <property name="minimum" value="2"/>
        </properties>
    </rule>

    <!-- MockMvc integration tests pull in get, status, content, jsonPath, and Hamcrest matchers. -->
    <rule ref="category/java/codestyle.xml/TooManyStaticImports">
        <properties>
            <property name="maximumStaticImports" value="12"/>
        </properties>
    </rule>

    <!-- Allow short names like Me, Login, Register, User; flag only single-letter classes. -->
    <rule ref="category/java/codestyle.xml/ShortClassName">
        <properties>
            <property name="minimum" value="2"/>
        </properties>
    </rule>

    <!-- Allow REST shorthand methods like me(); flag truly cryptic ones. -->
    <rule ref="category/java/codestyle.xml/ShortMethodName">
        <properties>
            <property name="minimum" value="2"/>
        </properties>
    </rule>

    <!-- ===================== Design ===================== -->
    <rule ref="category/java/design.xml">
        <!-- Tuned thresholds -->
        <exclude name="LawOfDemeter"/>
        <exclude name="LoosePackageCoupling"/>
    </rule>
    <rule ref="category/java/design.xml/CyclomaticComplexity">
        <properties>
            <property name="methodReportLevel" value="8"/>
            <property name="classReportLevel" value="40"/>
        </properties>
    </rule>
    <rule ref="category/java/design.xml/CognitiveComplexity">
        <properties>
            <property name="reportLevel" value="12"/>
        </properties>
    </rule>
    <rule ref="category/java/design.xml/NPathComplexity">
        <properties>
            <property name="reportLevel" value="120"/>
        </properties>
    </rule>
    <rule ref="category/java/design.xml/TooManyMethods">
        <properties>
            <property name="maxmethods" value="12"/>
        </properties>
    </rule>
    <rule ref="category/java/design.xml/ExcessiveParameterList">
        <properties>
            <property name="minimum" value="5"/>
        </properties>
    </rule>
    <rule ref="category/java/design.xml/ExcessivePublicCount">
        <properties>
            <property name="minimum" value="20"/>
        </properties>
    </rule>
    <!-- Spring tests with MockMvc + AssertJ + Jackson + Testcontainers easily reach 20+ imports. -->
    <rule ref="category/java/design.xml/ExcessiveImports">
        <properties>
            <property name="minimum" value="40"/>
        </properties>
    </rule>
    <!-- An aggregator that merges 20 fields across 5 sources references many types by nature. -->
    <rule ref="category/java/design.xml/CouplingBetweenObjects">
        <properties>
            <property name="threshold" value="30"/>
        </properties>
    </rule>

    <!-- ===================== Error Prone ===================== -->
    <rule ref="category/java/errorprone.xml"/>

    <!-- ===================== Magic Strings / Duplicate Literals ===================== -->
    <rule ref="category/java/errorprone.xml/AvoidDuplicateLiterals">
        <properties>
            <property name="maxDuplicateLiterals" value="2"/>
            <property name="skipAnnotations" value="true"/>
        </properties>
    </rule>
    <rule ref="category/java/errorprone.xml/AvoidLiteralsInIfCondition">
        <properties>
            <property name="ignoreMagicNumbers" value="-1,0,1"/>
        </properties>
    </rule>

    <!-- ===================== Multithreading ===================== -->
    <rule ref="category/java/multithreading.xml">
        <!-- Fires on every local Map, including method-local EnumMaps with no shared access where
             a ConcurrentHashMap would be slower and wrong. Concurrency is handled deliberately
             where it matters, not by defaulting every map to concurrent. -->
        <exclude name="UseConcurrentHashMap"/>
    </rule>

    <!-- ===================== Performance ===================== -->
    <rule ref="category/java/performance.xml"/>

    <!-- ===================== Security ===================== -->
    <rule ref="category/java/security.xml"/>

</ruleset>
